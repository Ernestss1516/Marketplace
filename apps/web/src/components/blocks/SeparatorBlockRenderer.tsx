import { Separator } from '@/components/ui/separator';

// Sin datos propios — puramente visual. Reutiliza el componente ya instalado.
export function SeparatorBlockRenderer() {
  return <Separator className="my-2" />;
}
