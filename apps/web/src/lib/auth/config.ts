// Placeholder — Auth.js (next-auth v5) configuration.
// Providers and adapters are wired up in the authentication implementation phase.

export const authConfig = {
  providers: [] as [],
  pages: {
    signIn: '/login',
    error: '/login',
  },
};
