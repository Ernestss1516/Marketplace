// Sin campos — el separador solo existe. El propio BlockEditorRow ya
// muestra el nombre/icono del tipo; aquí no hace falta nada más.
export function SeparatorBlockEditor() {
  return <p className="text-xs italic text-muted-foreground">Sin opciones — se muestra como una línea divisoria.</p>;
}
